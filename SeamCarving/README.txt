Description:

Program reads in photo data into RGB pixel data. Algorithm computes natural photo seam based on energy of pixels and autocrops image such that prominent features remain while non important details are cropped based on algorithm
